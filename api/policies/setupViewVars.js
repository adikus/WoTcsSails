/**
 * checkCache
 *
 * @module      :: Policy
 * @description :: Checks and removes expired cache
 *
 */
module.exports = function(req, res, next) {

    /*sails.config.view = {
        region: 1
    };*/

    next();

};
